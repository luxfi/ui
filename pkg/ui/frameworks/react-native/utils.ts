/**
 * React Native utilities
 *
 * @note Placeholder for React Native-specific utilities
 */

export const cn = (...args: any[]) => {
  // React Native doesn't use className, so this is a no-op
  return ''
}
