/**
 * Svelte stores
 *
 * @note Svelte-specific reactive stores for UI state
 * Requires svelte as a peerDependency
 */

// Placeholder - requires svelte package
export const theme = { subscribe: () => {} }
export const isMobileMenuOpen = { subscribe: () => {} }
