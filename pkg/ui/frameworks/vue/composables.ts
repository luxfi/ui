/**
 * Vue composables
 *
 * @note Vue Composition API utilities
 * Requires vue as a peerDependency
 */

// Placeholder - requires vue package
export const useTheme = () => {
  return {
    theme: { value: 'light' },
    isDark: { value: false },
    toggleTheme: () => {}
  }
}
