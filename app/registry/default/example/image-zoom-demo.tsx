import { ImageZoom } from "@/registry/default/ui/image-zoom"

export default function ImageZoomDemo() {
  return (
    <div className="w-full min-h-[400px] flex items-center justify-center">
      <ImageZoom />
    </div>
  )
}
